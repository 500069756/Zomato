# Frontend (Vercel) — copy to .env.local for local dev.
# This file is committed; never put real secrets here.

# URL of the Flask backend deployed on Render.
# Used server-side only by app/api/recommend/route.ts; not exposed to the browser.
# Local dev: http://localhost:8000
# Production: https://zomato-2-9s48.onrender.com
BACKEND_URL=http://localhost:8000
